\documentclass[11pt]{article}

\usepackage{hyperref}
\usepackage{ifthen}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage[labelsep=period,font=bf]{caption}
\usepackage[margin=1in, bottom=2.54cm]{geometry}

\newcommand{\changefont}{\fontsize{9}{11}\selectfont}

$if(csl-refs)$
\usepackage{calc}
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newlength{\csllabelwidth}
\setlength{\csllabelwidth}{3em}
\newenvironment{CSLReferences}[2]
{\begin{list}{}{\setlength{\leftmargin}{\cslhangindent}
			\setlength{\itemindent}{-\cslhangindent}
			\setlength{\itemsep}{0.5em}
			\setlength{\parsep}{0pt}}
		\renewcommand{\bibitem}[2][]{\item}}
	{\end{list}}
\newcommand{\citeproctext}{}
\newcommand{\CSLBlock}[1]{#1}
\newcommand{\CSLLeftMargin}[1]{#1}
\newcommand{\CSLRightInline}[1]{#1}
\newcommand{\CSLIndent}[1]{#1}
$endif$

\title{$title$}
\author{
$for(authors)$
$if(authors.name)$
$authors.name$
$else$
$authors$
$endif$
$sep$ \and
$endfor$
}
\date{$if(date)$$date$$else$\today$endif$}


$if(is_preprint)$
\newcommand{\PreprintTitle}{$title$}
\newcommand{\PreprintAuthors}{
	$for(by-author)$
	$by-author.name.literal$\\
	{\small $for(by-author.affiliations)$$it.name$$sep$, $endfor$}
	$sep$\\[0.5em]
	$endfor$
}
\newcommand{\PreprintDate}{$if(date)$$date$$else$\today$endif$}
\newcommand{\VersionLabel}{$if(version_label)$$version_label$$else$Preprint$endif$}
\newcommand{\CitationText}{$if(citation_text)$$citation_text$$else$[citation text not provided]$endif$}
$if(doi)$
\newcommand{\HasDOI}{1}
\newcommand{\PaperDOI}{$doi$}
$else$
\newcommand{\HasDOI}{0}
$endif$
$endif$

$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$3$endif$}
$else$
\setcounter{secnumdepth}{0}
$endif$

\begin{document}

$if(is_working_paper)$
\maketitle
$endif$

$if(is_preprint)$
\input{template/sections/cover_page.tex}
$endif$

$body$

\end{document}
