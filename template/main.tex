\documentclass[11pt]{article}

\usepackage{hyperref}
\usepackage{ifthen}

$if(csl-refs)$
\usepackage{calc}
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newlength{\csllabelwidth}
\setlength{\csllabelwidth}{3em}
\newenvironment{CSLReferences}[2]
 {\begin{thebibliography}{99}}
 {\end{thebibliography}}
\newcommand{\citeproctext}{}
\newcommand{\CSLBlock}[1]{#1}
\newcommand{\CSLLeftMargin}[1]{#1}
\newcommand{\CSLRightInline}[1]{#1}
\newcommand{\CSLIndent}[1]{#1}
$endif$

\title{$title$}
\author{
$for(authors)$
$if(authors.name)$
$authors.name$
$else$
$authors$
$endif$
$sep$ \and
$endfor$
}
\date{$if(date)$$date$$else$\today$endif$}


$if(is_preprint)$
\newcommand{\PreprintTitle}{$title$}
\newcommand{\PreprintAuthors}{$for(authors)$$if(authors.name)$$authors.name$$else$$authors$$endif$$sep$\\[0.2em]$endfor$}
\newcommand{\PreprintDate}{$if(date)$$date$$else$\today$endif$}
\newcommand{\VersionLabel}{$if(version_label)$$version_label$$else$Preprint$endif$}
\newcommand{\CitationText}{$if(citation_text)$$citation_text$$else$[citation text not provided]$endif$}
$if(doi)$
\newcommand{\HasDOI}{1}
\newcommand{\PaperDOI}{$doi$}
$else$
\newcommand{\HasDOI}{0}
$endif$
$endif$

\begin{document}

$if(is_working_paper)$
\maketitle
$endif$

$if(is_preprint)$
\input{template/sections/cover_page.tex}
\input{template/sections/citation_block.tex}
$endif$

$body$

\end{document}
